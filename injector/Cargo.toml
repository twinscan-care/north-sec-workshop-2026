[package]
name = "injector"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
libc = "0.2.178"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
